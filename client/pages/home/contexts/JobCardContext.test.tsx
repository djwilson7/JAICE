import { describe, it, expect } from "vitest";
import { JobCardContext } from "./JobCardContext";

describe("JobCardContext", () => {
  it("is created", () => {
    expect(JobCardContext).toBeTruthy();
  });
});
